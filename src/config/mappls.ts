// Mappls (MapmyIndia) Web Map SDK static key — from the Mappls console
// "Karmaverse" (Web) application. This is a client-side, domain-whitelisted key,
// so it is safe to ship in the web bundle. Its protection is the domain
// whitelist in the Mappls console (localhost + production domain must be listed).
export const MAPPLS_WEB_KEY = 'nqzgitfrmvqvmcpwrqsrpfcamgvpjyoqutrv';
